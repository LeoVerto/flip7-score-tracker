{
  "name": "flip7-score-tracker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env=''",
    "stage": "wrangler dev --env=staging",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
